# Supabase (browser-safe)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase (server-side only; bypasses Row Level Security)
SUPABASE_SECRET_KEY=

# AI (server-side only — never exposed to the browser)
OPENROUTER_API_KEY=
